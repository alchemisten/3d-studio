export enum LightType {
  Ambient = 'ambient',
  Directional = 'directional',
  Point = 'point',
  Spot = 'spot',
}

export enum MaterialType {
  Basic = 'basic',
  Lambert = 'lambert',
  Phong = 'phong',
  Physical = 'physical',
  Standard = 'standard',
}
